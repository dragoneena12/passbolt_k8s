apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: passbolt-db-pv-claim
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
